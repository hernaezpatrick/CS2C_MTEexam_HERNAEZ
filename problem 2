// File: classSubjects.js

// User Information Collection
let subjectTitle = "DSA";
let classSchedule = "8:30 AM, M-F";
let classroom = "Computer Lab B";
let classInstructor = "Owen Pascua";
let studentName = "Patrick Hernaez";

// Log user information to console
console.log(
    `${studentName} is currently enrolled in ${subjectTitle} with a class schedule of ${classSchedule} at room ${classroom}. The instructor for the subject is ${classInstructor}.`
);
